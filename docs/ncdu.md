# Ncdu Notes

## Location
- Stowed from `ncdu/dot-config/ncdu/` to `~/.config/ncdu/`.

## Content
- `config` for ncdu user-space preferences.

## Usage
- Preferred: `make stow`.
- One-package raw Stow: `stow --dotfiles --ignore='(\.DS_Store|README\.md)$' --target="$HOME" ncdu`.
