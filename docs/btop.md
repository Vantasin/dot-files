# Btop Notes

## Location
- Stowed from `btop/dot-config/btop/` to `~/.config/btop/`.

## Content
- `btop.conf` and related user-space settings.

## Usage
- Apply via Stow: `stow --dotfiles --target="$HOME" btop`.
- Restart btop to pick up changes.
