# Global Git configuration (user.name/user.email intentionally unset).

[init]
	defaultBranch = main

[core]
	excludesFile = ~/.config/git/ignore
	pager = less -FRX

[color]
	ui = auto

[pull]
	ff = only

[push]
	default = simple

[merge]
	conflictstyle = zdiff3
